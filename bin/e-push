#!/usr/bin/env sh

# The goal of this command is to capture the STDIN and insert it into:
#
# 1) the current clock; else 2) the current buffer

if [ -z "$1" ]; then
    text=$(</dev/stdin)
else
    text=$@
fi
$EDITOR --eval "(jf/capture/text-from-stdin \"$text\")"
exit 0
