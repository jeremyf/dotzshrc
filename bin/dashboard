#!/usr/bin/env ruby -w

ORG_LINK_REGEXP = /\[\[(?<url>.*)\]\[(?<title>.*)\]\](?<raw_tags>.*)/
File.readlines(File.join(ENV["HOME"],"/git/org/forem/dashboard.org")).each do |line|
  match = ORG_LINK_REGEXP.match(line)
  next unless match
  system("open #{match[:url]}")
end
